# 2026-06-28 F1-home-lint 首页历史代码清理

## 目标

把首页 `app/page.tsx` 里历史遗留的 `any`、旧组件、未使用代码单独拆出来清理，不再和视觉调整混在一起。

## 已处理

- 删除不再渲染的旧首页函数：
  - 旧 canvas hero。
  - 统计条。
  - 8 卡功能区。
  - 数据分析工作台展示区。
  - 积分生态区。
  - 智能产品搜索区。
  - 使用流程。
  - 视频教程。
  - ELISA 方法学。
  - 每日知识。
  - 社区区块。
- 删除旧编辑模式相关逻辑和旧 `Navbar` 导入。
- 删除未使用图标导入。
- 删除未使用的 `saveToLocal`。
- `supabase` 类型从 `any` 改为 `SupabaseClient | null`。
- 新增 `HomepageContent`，当前首页只保留页脚版权字段。
- `Footer` props 改为明确类型，去掉 `any`。

## 结果

- `app/page.tsx` 变更量：删除 740 行，新增/保留 163 行当前首页实际结构。
- `npm run lint -- app/page.tsx`：通过。
- `npm run build`：通过。
- `npm run deploy:aliyun`：通过，服务已重启并处于 running。
- 线上健康检查：22 个页面和 4 个 API 正常。
- 线上首页确认仍包含新版精简内容：`快速进入核心服务`、`产品信息保持清晰`、`需要资料、选型或实验支持`。
- 本地复查：`app/page.tsx` 当前 328 行，未检出旧首页函数、`any`、`saveToLocal`。

## 后续建议

- F1-home-copy：等用户确认首页新版方向后，继续精修文案和视觉节奏。
- F1-footer-settings：页脚联系方式后续接入后台设置，避免硬编码。
