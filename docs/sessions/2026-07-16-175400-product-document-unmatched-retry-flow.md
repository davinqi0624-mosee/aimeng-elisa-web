# 2026-07-16 17:54 产品文档未匹配重传流程调整

## 背景

用户希望：

- 撤回本次 181 个文件批次。
- 未匹配和失败文件必须清晰列出。
- 未匹配文件不要归档，也不要继续存放在网站存储里。
- 确认上架前台的文件要和未匹配、失败文件区分开。

## 已处理

### 当前批次处理

批次 `ae842ad8-c96d-424a-ace1-e1adb4f4e2c1` 已撤回：

- 原状态：66 个 pending，115 个 archived。
- 处理后：181 个全部 archived。
- 该批次 181 个文件的存储副本已删除。
- 后台记录保留，备注为：管理员撤回整个批次，文件已从存储删除；请重新上传需要上架的文件。

### 后续流程调整

- 文件名有明确货号但产品库没有该货号时，不再走名称相似匹配，避免错误匹配到其他货号。
- 未匹配文件保留后台记录，状态仍为待处理/需重传，但 PDF 文件会从存储删除。
- 重复上传文件也会删除重复 PDF 副本，只保留后台记录说明原因。
- 上传失败文件会写入批次备注，刷新后仍可看到失败清单。
- 后台列表中，已删除存储文件的记录不再显示“查看文件”，改显示“文件已删除”。
- 未匹配记录显示为“需重传”，用于和“待上架”区分。

## 验证

- `npm run build` 通过。
- `npm run deploy:aliyun` 已部署到 `http://106.14.215.238`。
- 健康检查通过。
