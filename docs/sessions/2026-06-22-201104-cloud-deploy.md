# 2026-06-22 云服务器部署记录

## 基本信息

- 服务器 IP：106.14.215.238
- SSH 私钥：`/Users/moses/aimeng-elisa-web/爱萌优宁.pem`
- SSH 用户：`root`
- 系统：Ubuntu 24.04.4 LTS
- Node.js：v22.23.0
- nginx：1.24.0
- 本地提交：`0e0d527`

## 部署方式

- Next.js 版本：16.2.4
- 构建模式：`output: 'standalone'`
- 本地构建命令：`npm run build`
- 线上应用目录：`/opt/aimeng-elisa-web/app`
- 线上环境变量：`/etc/aimeng-elisa-web/aimeng-elisa-web.env`
- 应用服务：`aimeng-elisa-web.service`
- 对外访问：`http://106.14.215.238`
- Next.js 进程监听：`127.0.0.1:3000`
- nginx 对外监听：`80`

## 本次处理

1. 安装服务器运行时：Node.js 22、npm、nginx。
2. 使用本地 standalone 构建产物部署到服务器。
3. 配置 systemd 托管 Next.js 服务，并设置开机自启。
4. 配置 nginx 反向代理到 `127.0.0.1:3000`。
5. `.env.prod` 中关键 Supabase/API 值为空，本次使用 `.env.local` 写入线上环境文件。
6. 修复首次部署时 `/var/www/aimeng-elisa-web` 残留目录和孤儿 Next 进程导致的 manifest 缺失问题，最终切换到 `/opt/aimeng-elisa-web/app`。
7. 清理旧的 `/var/www/aimeng-elisa-web` 残留目录。

## 验证结果

- `npm run build`：通过
- `HEALTH_BASE_URL=http://106.14.215.238 npm run health`：通过
- 健康检查覆盖：22 个页面、4 个 API，全部通过
- 抽查路径：
  - `/`
  - `/search`
  - `/products/elisa`
  - `/chat`
  - `/login`
  - `/api/knowledge/daily?all=true`

## 常用运维命令

```bash
ssh -i /Users/moses/aimeng-elisa-web/爱萌优宁.pem root@106.14.215.238
systemctl status aimeng-elisa-web
systemctl restart aimeng-elisa-web
journalctl -u aimeng-elisa-web -n 100 --no-pager
systemctl status nginx
nginx -t
ss -ltnp
```

## 注意事项

- 线上环境文件包含密钥，权限为 `600`，不要打印内容。
- 服务器当前只有 HTTP 80，后续绑定域名后再配置 HTTPS。
- 如果再次部署，优先复用 `/opt/aimeng-elisa-web/app` 和现有 systemd/nginx 配置。
