# Git 仓库同步记录

日期：2026-09-03

## 处理内容

- 确认本项目为 Git 仓库，当前分支为 `main`。
- 确认远程仓库为 `origin/main`。
- 将当前网站源代码、数据库迁移、必要的公共资源、部署脚本和会话记录纳入版本控制。
- 将本机业务资料、上传测试 PDF、报告备份、环境文件、私钥和本地工具配置排除在 GitHub 之外。
- 从版本控制中移除 Supabase CLI 的本地临时版本文件。
- 从 `AGENTS.md` 移除服务器 IP 与私钥路径，仅保留安全说明。

## 验证

- `git diff --check`：通过。
- `npm run build`：通过。
- Next.js 生产构建完成，页面和 API 路由均成功生成。

## 说明

生产环境密钥仍需保存在 Vercel、服务器或本机安全环境变量中，不通过 Git 同步。业务资料如果需要备份，应使用独立的文件备份或对象存储方案。
